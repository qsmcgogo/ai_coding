function createInitialState(rows, cols) {
}

function generateFood(snake, rows, cols) {
}

function changeDirection(currentDirection, newDirection) {
}

function getNextHeadPosition(head, direction) {
}

function checkCollision(head, snake, rows, cols) {
}

function moveSnake(snake, nextHead, ateFood) {
}

function calculateScore(currentScore, snakeLength) {
}

if (typeof module !== 'undefined' && module.exports) {
    module.exports = {
        createInitialState,
        generateFood,
        changeDirection,
        getNextHeadPosition,
        checkCollision,
        moveSnake,
        calculateScore
    };
}
